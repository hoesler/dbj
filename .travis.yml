language: r

cache:
  - packages
  - directories:
    - $HOME/.m2

before_install:
  - if [ -n "$GITHUB_PAT" ]; then echo "GITHUB_PAT=\"$GITHUB_PAT\"" >> ~/.Renviron; else echo "GITHUB_PAT is not set"; fi

r_github_packages:
  - hoesler/DBItest@dbi-0.3.1/1.1

branches:
  except:
    - appveyor
